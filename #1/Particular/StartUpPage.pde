class StartUpPage
{ 
  boolean mouseFlag;
  
  int textColour = 255;
  
  void show()
  {
    background(0);
    fill(255);
    textSize(20);
    textAlign(CENTER);
    text("Startup Page", width / 2, height / 2);
  }
}